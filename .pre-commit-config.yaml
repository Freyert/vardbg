repos:
  - repo: local
    hooks:
      - id: isort
        name: isort
        stages: [commit]
        language: system
        entry: uv run isort -y
        types: [python]

      - id: black
        name: black
        stages: [commit]
        language: system
        entry: uv run black
        types: [python]
